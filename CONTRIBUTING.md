# Contributing

Keep changes small and focused.

Before opening a pull request, run:

```sh
npm run lint
npm test
npm run build
```

Do not commit generated folders, local data, dependency folders, or backup archives.
